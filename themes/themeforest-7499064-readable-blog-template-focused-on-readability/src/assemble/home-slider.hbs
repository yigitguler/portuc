{{> head}}
{{> header}}
{{> slider}}
<div class="container">
  <div class="row">
    <div class="col-xs-12  col-md-8">
      {{> post-with-featured-image}}
      {{> post-with-video}}
      {{> post-without-featured-image}}
    </div>
    <div class="col-xs-12  col-md-4">
      {{#withFirst db.authors_left}}
        {{> widget-author}}
      {{/withFirst}}
      <div class="sidebar  boxed  push-down-30">
        <div class="row">
          <div class="col-xs-10  col-xs-offset-1">
            {{> widget-categories}}
            {{> widget-featured-post}}
            {{> widget-facebook}}
            {{> widget-posts}}
            {{> widget-tags}}
            {{> widget-twitter}}
            {{> widget-recent-comments}}
            {{!-- {{> widget-banner}} --}}
            {{> widget-social-icons}}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{{> footer}}
{{> foot}}